name: smart_device_tester
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Iconos y librerías adicionales
  cupertino_icons: ^1.0.8
  syncfusion_flutter_gauges: ^31.2.15
  flutter_bluetooth_serial: ^0.4.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Librerías de desarrollo (Corrección aplicada aquí)
  mocktail: ^1.0.0
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true